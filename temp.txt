func calc(x: int; p: real*) return int {
    var ptr: int*;
    ptr = &x;
    ^ptr = 0x1A; /% מספר הקסדצימלי %/
    return ^ptr;
}

proc Main() {
    var s: string[50];
    var flag: bool;
    var res: int;

    s = "Hello World";
    flag = true;

    if (flag != false) {
        res = calc(100, null);
    }
}